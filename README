Description: Provide panel menu for favorites. Modified version which allows you to show an icon instead of the Favorites text, as well as showing both the icon and text. Also added a tooltip for the icon mode, and Icon mode is default.


~1 click install from E.G.O: https://extensions.gnome.org/extension/201/favorites-menu-plus/
